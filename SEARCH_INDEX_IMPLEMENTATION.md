# Termflow 全文搜索与本地索引持续实施文档

> 状态：进行中  
> 建立日期：2026-08-14  
> 文档版本：0.16.0
> 维护规则：实现、测试、验收和后续迭代必须同步更新本文档。

## 1. 文档目的

本文档是 Termflow 全文搜索与本地索引功能的实施基线，用于持续记录：

- 产品行为与兼容边界；
- 架构决策及其原因；
- 分阶段任务和验收标准；
- 已知风险、测试矩阵和性能数据；
- 每一轮迭代的完成情况、遗留问题与下一步计划。

任何实现如果偏离本文档，应先在“决策记录”中说明原因，再修改代码。任何阶段只有在对应测试和验收项通过后才能标记完成。迭代日志中的“未实现”仅描述该次迭代结束时的历史快照；当前行为以本节和里程碑检查项为准。

## 2. 当前问题与基线

最初的全文搜索由 `src-tauri/src/commands/content_search.rs` 实现，采用单任务逐目录、逐文件扫描：

1. 使用 `fs::read_dir` 遍历目录；
2. 针对每个路径调用 libgit2 判断 ignore；
3. 使用 `fs::read` 把整个文件读入内存；
4. 仅接受完整 UTF-8 文件；
5. 逐行执行正则匹配并复制上下文；
6. 每 200 条结果向前端发送一个批次。

截至文档版本 0.16.0，快速扫描已改为 `ignore::WalkParallel` + `grep-searcher`；手动开启项目索引会启动真实 FTS5 trigram 后台构建。普通长度至少 3 的文字查询在索引 ready 时使用 FTS5 候选集并由统一 matcher 精确验证；短查询、正则、stale、构建中和异常场景自动回退快速扫描。项目 watcher 会将普通文件 create/modify/remove/rename 合并为单个 SQLite 事务的增量更新；超过 1,024 个路径的批次、ignore 规则、目录事件、watcher 溢出、错误和增量失败则安全回退到全量原子重建，并在 2 秒静默期后执行。损坏索引会隔离为本地缓存文件后自动重建。索引构建进度已进入全局底栏后台任务入口，可与一个或多个 Git 克隆任务独立并行显示。全文搜索窗口采用 IDEA 风格上下结构，上方浏览结果、下方预览上下文，中间分隔条支持鼠标拖动调整比例，双击恢复默认 55/45。

已观察到的真实基线：

- 项目文件数：22,317；
- 匹配结果：94 条，分布于 17 个文件；
- 总耗时：330.45 秒；
- 平均处理速度：约 67.5 文件/秒。

当前“索引库”设置页支持当前项目启用/关闭、构建/重建和删除索引，以及文件进度、跳过数、索引大小、更新时间和错误原因；短暂的增量更新显示为“同步中”。已实现 FTS5 查询路由、缓存目录迁移、容量上限与 LRU 清理、清空全部索引缓存，以及普通文件的批量增量写入。尚未实现自动索引新项目、暂停/恢复，以及 watcher 溢出后的周期性 reconciliation。

## 3. 总体目标

### 3.1 功能目标

- 未建立索引时仍可可靠搜索；
- 索引建立中、过期、损坏或不可用时自动降级；
- 支持普通字符串、大小写、全词和正则模式；
- 支持包含/排除 glob、Git ignore、普通目录和非 Git 项目；
- 结果流式返回，搜索可快速取消；
- 索引完全保存在本机，不写入代码库；
- 索引可以启用、关闭、重建和清空全部缓存，并有清晰状态；暂停/恢复和单项目删除仍在规划中；
- Windows 优先，同时保持 macOS/Linux 可编译和可测试。

### 3.2 性能目标

以下指标是验收目标，不是未经测量的承诺：

| 场景 | 目标 |
| --- | --- |
| 20k 文件、普通未命中查询、快速扫描、热缓存 | 完整搜索不超过 3 秒 |
| 索引已就绪、普通长度 ≥ 3 查询 | 首批结果不超过 100ms |
| 用户取消搜索 | 100ms 内停止继续产生结果 |
| 搜索期间 UI | 不阻塞输入、关闭和项目切换 |
| 索引构建期间 | 搜索自动使用快速扫描，不等待构建 |

性能目标必须在真实磁盘和生成的固定测试仓库上分别记录；CI 只验证相对回归，不使用绝对时间判定。

## 4. 非目标

首个稳定版本不处理：

- 压缩包内部搜索；
- PDF、Office、图片 OCR 等非源码内容提取；
- 跨设备或云端同步索引；
- 语义搜索、向量搜索或自然语言代码问答；
- 默认跟随可能越出项目根目录的符号链接；
- 正则 look-around、反向引用等 Rust 线性正则不支持的语法。

## 5. 架构原则

### 5.1 双引擎

搜索必须包含两条完整路径：

1. **快速扫描引擎**：基于 ripgrep 的 Rust 组件完成并行遍历、ignore 处理和流式匹配；
2. **索引引擎**：基于 SQLite FTS5 trigram 生成候选文件，再由统一匹配器进行精确验证。

索引只负责缩小候选集合，最终结果必须由与快速扫描相同的匹配器验证，避免大小写、全词、正则和索引快照差异导致错误结果。

### 5.2 任何时候可降级

出现以下任一情况时使用快速扫描：

- 项目未启用索引；
- 首次索引尚未完成；
- 索引状态为 stale、failed、corrupt 或 incompatible；
- 查询长度不足 3 个 Unicode 字符；
- 正则无法安全提取必要字面量；
- 索引候选数量超过安全阈值；
- SQLite/FTS5 运行时能力不足；
- 文件系统监听不可用且索引新鲜度无法保证。

降级不能静默发生，搜索摘要必须返回实际后端及原因。

### 5.3 单一过滤规则

快速扫描、首次索引、增量索引和元数据校验必须共用同一套过滤模块，统一以下语义：

- 内置目录排除；
- `.gitignore`、`.git/info/exclude`、全局 Git ignore；
- `.ignore` 和 `.rgignore`；
- 用户 include/exclude glob；
- 隐藏文件、符号链接、最大文件大小和二进制检测。

禁止分别维护多份排除目录或 glob 匹配实现。

## 6. 模块规划

计划将后端拆分为：

```text
src-tauri/src/commands/content_search/
├── mod.rs          # Tauri 命令和请求生命周期
├── types.rs        # IPC 类型与搜索摘要
├── matcher.rs      # 普通字符串、大小写、全词和正则匹配
├── filters.rs      # ignore、glob、文件大小和路径规则
├── scanner.rs      # 快速扫描引擎
└── tests.rs        # 跨模块测试

src-tauri/src/search_index/
├── mod.rs          # IndexManager 和项目状态
├── database.rs     # FTS5 schema、版本和完整性检查
├── builder.rs      # 首次构建和原子替换
├── updater.rs      # 增量更新队列
├── watcher.rs      # 文件事件、合并和重扫
└── query.rs        # 候选生成与混合搜索
```

前端继续使用 `content-search-batch` 接收结果，新增索引状态事件，不把搜索逻辑放入 React。

## 7. 快速扫描引擎要求

- 使用 `ignore::WalkBuilder::build_parallel()`；
- 使用 `grep-searcher` 和 `grep-regex`，不依赖用户安装的 `rg` 可执行文件；
- 默认不跟随符号链接；
- 默认遵循标准 ignore 文件；
- 用户 exclude 始终优先于 include；
- 支持最大文件大小，默认 5 MiB；
- 检测二进制文件并跳过；
- 使用有界结果通道，避免前端消费慢导致内存持续增长；
- 每个搜索拥有独立取消令牌；
- 并行结果允许乱序，搜索结束后由前端稳定排序；
- 匹配数达到上限后立即取消剩余遍历；
- 错误按“单文件可跳过、根目录/请求不可恢复”分类。

## 8. 索引存储设计

索引数据库与主设置数据库分离，计划路径：

```text
<app_cache_dir>/search-index/v1/<project-key>/index.sqlite3
```

其中 `project-key` 来自规范化项目路径的 SHA-256。数据库不得写入代码仓库。

建议 schema：

```text
index_meta
- schema_version
- generation
- build_complete
- root_fingerprint
- ignore_fingerprint
- last_full_scan_at

indexed_files
- id
- relative_path
- size
- modified_ns
- content_hash
- encoding
- indexed_at
- status

content_fts
- contentless-delete FTS5
- tokenize = trigram
- 使用默认 detail，以支持长度超过 3 个字符的 trigram 短语/子串查询
- rowid 对应 indexed_files.id
```

必须在运行时检测 SQLite 版本和 FTS5 能力。无法创建所需虚拟表时将项目标记为 unsupported，并继续提供快速扫描。

首次构建写入临时数据库，完成完整性检查后原子替换正式数据库；不能向正在使用的正式索引暴露半成品。

## 9. 查询路由

| 查询 | 路由 |
| --- | --- |
| 普通字符串，长度 ≥ 3 | trigram 候选 + 精确验证 |
| 普通字符串，长度 < 3 | 快速扫描 |
| 大小写敏感 | 索引候选 + 大小写精确验证 |
| 全词 | 索引候选 + 边界精确验证 |
| 可提取必要字面量的正则 | 索引候选 + 正则精确验证 |
| 无法安全预筛的正则 | 快速扫描 |
| 索引候选过多 | 快速扫描或混合扫描 |

正则候选提取必须保守：不确定时降级，不能产生假阴性。

## 10. 增量索引与一致性

- 每个项目只有一个串行索引写入 actor；
- 创建/修改触发重新读取和重新索引；
- 删除移除文件记录和 FTS 行；
- 重命名按删除旧路径、添加新路径处理；
- 300～500ms 内的重复事件合并；
- 构建期间的事件写入变更日志，构建完成后重放；
- `.gitignore`、`.ignore`、`.rgignore` 和项目过滤设置变化触发范围核对；
- Termflow 自身的写文件操作直接入队，不只依赖 watcher；
- `notify::Event::need_rescan()` 为真时执行全量元数据核对；
- 项目打开时和空闲期间执行低频元数据 reconciliation；
- 网络文件系统使用 PollWatcher 或保持扫描模式；
- 索引 stale 时不能只搜索已知索引并假装结果完整。

## 11. 编码、路径和安全边界

### 11.1 编码

- 支持 UTF-8、UTF-8 BOM；
- 支持带 BOM 的 UTF-16 LE/BE；
- 未知编码默认跳过并计入摘要；
- 不使用高风险猜测把任意二进制内容当文本；
- 行号和列号继续使用前端兼容的 UTF-16 列计数。

### 11.2 路径

- 内部使用 `PathBuf`，数据库存相对路径；
- Windows 比较路径时处理大小写不敏感和长路径；
- 不存在的路径不能调用依赖存在性的 canonicalize 后再报模糊错误；
- 符号链接默认不跟随；未来若允许跟随，必须防循环并阻止越出项目根目录；
- Unix 非 UTF-8 路径首版跳过并报告，不使用损失性转换建立不可逆索引键。

### 11.3 资源限制

- 默认最大文件：5 MiB；
- 默认最大结果：5,000；
- 正则编译设置大小限制；
- 索引构建使用有界队列和批量事务；
- 索引磁盘不足时删除临时文件并回退扫描；
- 不在日志、遥测中记录查询正文和文件内容。

## 12. 设置页行为

“索引新文件夹”是全局策略：首次打开项目时预扫描符合条件的文件。如果文件数、总字节数、预计索引大小、剩余磁盘和文件系统类型都符合条件，才自动启用。

“索引代码库以实现即时搜索”是项目级设置，设置页需要展示项目列表而不是一个无上下文的全局开关：

```text
项目       状态       文件数     索引大小      操作
renmin     已就绪     22,317     186 MiB       重建 / 删除 / 关闭
Termflow   构建中     1,284      24 MiB        暂停
```

必须展示：preflight、building、ready、updating、stale、paused、failed、unsupported 状态，以及文件数、占用、最后更新时间和最近错误。

用户手动关闭某项目后，应记录明确覆盖值，不能被“自动索引新文件夹”再次打开。

## 13. IPC 规划

保留：

- `search_project_text`
- `cancel_content_search`
- `content-search-batch`

扩展搜索摘要：

- `backend`: `scan | index | hybrid`
- `fallbackReason`
- `candidateFiles`
- `indexGeneration`
- `indexState`

新增命令：

- `list_search_indexes`
- `set_project_index_enabled`
- `rebuild_project_index`
- `pause_project_index`
- `delete_project_index`
- `get_search_index_status`

新增事件：

- `search-index-status`

## 14. 测试矩阵

### 14.1 单元测试

- 普通字符串转义；
- 大小写、全词和 Unicode 边界；
- include/exclude 优先级；
- `.gitignore`、`.ignore`、全局 ignore；
- 二进制和最大文件；
- UTF-8 BOM、UTF-16、无效编码；
- 路径规范化和 scope 越界；
- 取消、结果上限和错误分类；
- 正则候选提取不会产生假阴性；
- schema 版本、损坏检测和重建。

### 14.2 集成测试

- Git 与非 Git 临时项目；
- 创建、修改、删除和重命名；
- 构建期间发生变更；
- Git checkout/pull 产生大量变化；
- watcher 丢事件和强制 rescan；
- 索引数据库损坏、只读和磁盘写入失败；
- 多搜索并发与取消；
- 索引关闭、构建中、过期时的降级；
- Windows/macOS/Linux 路径差异。

### 14.3 性能测试

固定生成 1k、20k、50k 文件仓库，覆盖：

- 冷缓存和热缓存；
- 无匹配、少量匹配、5,000+ 匹配；
- 普通字符串、短字符串、中文和正则；
- 首次构建、增量更新和完全重建；
- 索引大小和峰值内存。

## 15. 开源与发布要求

- 不依赖用户安装外部搜索程序；
- 新依赖必须记录用途并检查许可证；
- Windows 发布不新增难以维护的 sidecar；
- Rust 核心在 Windows、macOS、Linux CI 编译和测试；
- 索引格式升级不阻塞应用启动；
- 索引是可删除缓存，主数据库损坏域与索引损坏域分离；
- 用户文档说明哪些文件默认不搜索，以及如何诊断 ignore 行为。

## 16. 里程碑与检查清单

### M0：基线与实施文档

- [x] 记录真实慢搜索基线；
- [x] 建立持续实施文档；
- [x] 为现有 matcher、glob 和搜索摘要补齐测试；
- [x] 建立可重复的搜索测试仓库生成器；
- [x] 记录当前实现在固定 1k/20k/50k 仓库上的结果基线。

### M1：高性能快速扫描

- [x] 引入并审计 `ignore` 0.4.32（`Unlicense OR MIT`）；
- [x] 引入 `globset` 0.4，并复用 `ignore` 已锁定的兼容版本；
- [x] 引入并审计 `grep-searcher`、`grep-regex`；
- [ ] 抽取共享 types、matcher 和 filters；
- [x] 使用 `ignore::WalkBuilder` 替换手写目录遍历和逐路径 libgit2 ignore 查询；
- [x] 使用并行 walker 替换手写目录遍历；
- [x] 使用流式 searcher 替换整文件读取；
- [x] 保留上下文、UTF-16 列号、批次和结果上限行为；
- [ ] 实现有界结果通道和快速取消；
- [x] 新旧实现结果一致性测试通过；
- [x] 记录 1k/20k/50k 性能数据。

### M2：手动索引 MVP

- [x] 建立独立索引目录和项目 key；
- [x] 运行时检查 SQLite FTS5 能力；
- [x] 实现 schema 和格式版本；
- [x] 实现 preflight；
- [x] 实现临时构建、完整性检查和原子替换；
- [x] 实现 trigram 候选查询和精确验证；
- [x] 实现扫描自动降级；
- [x] 设置页支持当前项目启用、关闭和重建；
- [x] 支持单项目删除索引；
- [x] 搜索摘要显示实际后端。

### M3：增量更新与恢复

- [x] 实现索引专用 watcher；
- [x] 实现普通文件事件合并、串行批量 FTS5 upsert/delete；
- [x] 实现构建期间变更重放（通过安全全量重建）；
- [x] 实现 ignore 变化后的全量范围核对；
- [x] 实现打开项目 reconciliation（当前通过后台全量重建）；
- [x] 处理 `need_rescan` 并触发全量重建；
- [x] 原生 watcher 不可用时回退 PollWatcher；
- [x] 实现大批量事件阈值与静默期 reconciliation；
- [x] 实现损坏隔离和自动重建；
- [ ] 实现暂停、恢复和错误状态。

### M4：自动索引与存储管理

- [ ] 实现新项目自动索引策略；
- [ ] 同时评估文件数、字节数、预估大小和剩余空间；
- [x] 实现全局缓存预算和 LRU 清理；
- [ ] 实现项目列表、索引大小和最后更新时间；
- [ ] 记录用户手动覆盖值；
- [ ] 完成用户文档和故障排查。

### M5：跨平台与发布验收

- [ ] Windows、macOS、Linux 核心 CI；
- [ ] 大小写敏感/不敏感文件系统验证；
- [ ] Unicode、长路径和权限错误验证；
- [ ] 新依赖许可证检查；
- [ ] 性能回归报告；
- [ ] 发布说明和索引升级策略。

## 17. 迭代协议

每轮迭代必须执行：

1. 从最早未完成里程碑选择一个可独立验收的任务；
2. 在开始前记录本轮目标和非目标；
3. 实现并添加对应测试；
4. 运行受影响测试、Rust 编译检查和必要的前端检查；
5. 将数据、失败和取舍写入迭代日志；
6. 更新检查清单；
7. 明确下一轮唯一优先任务。

禁止为了勾选任务而跳过测试；无法完成时保留未完成状态并记录阻塞原因。

## 18. 决策记录

### D-001：使用双引擎而不是只实现索引

- 日期：2026-08-14
- 决策：快速扫描永久保留，索引是加速层。
- 原因：短关键词、复杂正则、索引失效、网络盘和首次构建都需要完整后备路径。

### D-002：内嵌 ripgrep Rust 组件而不是调用外部 `rg`

- 日期：2026-08-14
- 决策：使用 `ignore`、`grep-searcher`、`grep-regex`、`globset`。
- 原因：避免外部安装、sidecar、版本差异和命令行转义问题，保持跨平台构建一致。

### D-003：索引与主数据库分离

- 日期：2026-08-14
- 决策：每个项目使用独立的缓存数据库。
- 原因：索引体积大、可重建且写入频繁，不应扩大设置和会话数据库的锁竞争与损坏范围。

### D-004：FTS5 trigram 只作为候选生成器

- 日期：2026-08-14
- 决策：候选结果必须经过统一匹配器验证。
- 原因：支持代码子字符串，同时确保大小写、全词、正则和实时文件内容语义一致。

### D-005：请求级预编译 glob

- 日期：2026-08-14
- 决策：include/exclude 在请求开始时编译为大小写不敏感的 `globset`，文件名模式和相对路径模式分别匹配。
- 原因：避免为每个路径、每个 pattern 重复分配动态规划状态，同时为快速扫描、首次索引和增量索引提供同一过滤对象。非法 glob 在搜索开始前返回明确错误。

### D-006：候选扫描与精确结果计算分层

- 日期：2026-08-14
- 决策：`grep-regex` 负责流式定位候选行，已有 `regex::Regex` 继续负责候选行内的全部精确命中、UTF-16 列号和最终结果语义。
- 原因：`grep-searcher` 的 Sink 以匹配行为单位回调，而 UI 需要同一行内的每个命中。分层后既避免整文件读取，也不改变 IPC 结果粒度；候选器启用逐行 anchor 与 CRLF 兼容，并由精确匹配器消除多余候选。

### D-007：索引开关先保存项目意图，不伪造 ready 状态

- 日期：2026-08-14
- 决策：允许用户提前开启当前项目的索引偏好，但在 FTS5 引擎完成前返回 `scan_only`，搜索继续使用快速扫描；自动索引开关仍禁用。
- 原因：用户需要立即操作和验证设置页，同时偏好必须跨重启保存。将偏好状态与索引运行状态分离，可以避免“开关已打开”被误解为索引已经构建完成，也为后续 M2 构建任务保留稳定 IPC。

### D-008：FTS5 trigram 不使用 `detail=none`

- 日期：2026-08-14
- 决策：索引继续使用 contentless-delete + trigram，但保留默认 detail。
- 原因：真实 SQLite 验证表明，`detail=none` 不支持 trigram 为长度超过 3 的代码子串生成的短语查询；例如 `updateWrapper` 会直接查询失败。索引体积会因此增大，但任意代码子串可查询是功能正确性的前提，空间预算留到 M4 处理。

### D-009：第一版 watcher 变化同步使用防抖全量重建

- 日期：2026-08-17
- 决策：文件系统事件立即把 ready 索引标为 stale 并切回扫描，500ms 合并事件后启动全量原子重建；构建期间的 revision 变化会在完成后自动再构建一轮。
- 原因：逐文件增量更新必须完整复现层级 `.gitignore`、`.ignore`、`.rgignore`、删除、目录重命名、Git checkout 和 watcher 丢事件语义。先用全量重建建立不漏结果的一致性闭环，后续可在不改变状态机和查询路由的前提下把普通事件优化成批量 SQL upsert。

### D-010：底栏使用聚合后台任务模型，不复用单一进度状态

- 日期：2026-08-17
- 决策：Git 克隆任务继续以 `clone:<task-id>` 语义独立存在，当前项目索引以项目路径语义独立存在；底栏只负责聚合数量、紧凑展示和详情弹层，不让某类任务覆盖另一类任务状态。
- 原因：克隆和索引可能同时运行，也可能同时存在多个克隆。复用一个 `progress` 字段会造成后到事件覆盖、错误的取消目标和完成后过早隐藏。独立状态让并发只影响磁盘吞吐，不影响 UI 正确性或索引数据库原子替换。

### D-011：全文搜索窗口采用上下结果/预览结构

- 日期：2026-08-17
- 决策：搜索条件下方使用单列双行布局，上方 55% 显示结果列表，下方 45% 显示选中结果的上下文预览；两个区域独立滚动，窄窗口也不隐藏预览。
- 原因：上下结构更接近 IDEA 的 Find in Files 工作流，让结果行获得完整横向空间，长代码行、文件路径和预览代码不再争抢左右宽度，也更适合代码内容横向阅读。

### D-012：上下分隔条可调整且保留安全边界

- 日期：2026-08-17
- 决策：默认结果/预览比例为 55/45，鼠标拖动范围限制为 20/80～80/20，双击恢复默认比例；分隔条不提供方向键和 Home/End 调整入口。
- 原因：不同搜索任务需要不同的结果密度和预览高度，但无限拖动会把一侧压成不可操作区域。指针捕获保证拖出分隔条后仍连续调整；取消可聚焦的键盘调整入口，可避免分隔条与搜索结果的 ↑/↓ 导航形成两套相似但作用不同的操作。

## 19. 迭代日志

### Iteration 12 — 2026-08-17 — 文档状态校正

**完成**

- 将“当前问题与基线”改为当前运行时能力的摘要，明确 FTS5 查询路由、缓存目录迁移、容量上限、LRU 清理和清空全部缓存已实现；
- 将自动索引、单项目删除、暂停/恢复、逐文件增量写入和 watcher 溢出后的周期性 reconciliation 保留为未完成项；
- 修正 M2、M3、M4 检查项，并明确迭代日志中的遗留项是历史快照，不与当前状态混淆。

**说明**

- 本轮仅修正文档，不改变搜索运行时行为。

### Iteration 13 — 2026-08-17 — 批量增量索引更新

**完成**

- 将索引 schema 升级为 v2；`indexed_files` 同时保留已索引与跳过文件的状态，因此文件变成二进制、过大或被删除时仍能精确更新统计；旧 schema 会安全重建；
- watcher 在 500ms 合并窗口内收集普通文件 create/modify/remove/rename，并用单个 SQLite 事务执行 FTS5 delete/upsert；更新期间状态为 `updating`，搜索安全使用快速扫描；
- `.gitignore`、`.ignore`、`.rgignore`、`.git/info/exclude`、目录事件、`need_rescan`、watcher 错误、增量 SQL 失败和删除目录仍切换到 stale，并自动走全量原子重建；
- 新增修改、创建、删除和重命名的增量索引测试；相关 Rust 测试 10/10 通过。

**遗留与下一轮唯一优先任务**

- 为克隆或 Git checkout 的大批量事件增加阈值与延迟 reconciliation；随后实现索引损坏隔离和自动重建。

### Iteration 14 — 2026-08-17 — 大批量 reconciliation 与损坏恢复

**完成**

- 普通 watcher 批次最多增量写入 1,024 个路径；超过阈值时立即转为 stale / scan，并等待 2 秒没有新事件后执行一次全量原子 reconciliation，避免 Git checkout 或克隆期间反复重建；
- `.gitignore`、目录和 `need_rescan` 等结构性事件现在会立即标记 stale，确保不会错误保留 ready 查询路径；
- 打开索引或执行索引查询前运行 SQLite `quick_check`。检测到损坏时，将 `index.sqlite3` 隔离为 `index.corrupt-<timestamp>.sqlite3`，随后自动重建；不可用或查询异常同样会安全回退扫描并请求重建；
- 新增大批量阈值和损坏隔离测试；相关 Rust 测试 12/12 通过。

**下一轮唯一优先任务**

- 实现项目级索引删除，并在设置页提供明确的删除操作与确认提示。

### Iteration 15 — 2026-08-17 — 删除单项目索引

**完成**

- 新增 `delete_project_index` IPC：仅删除由项目路径哈希导出的专属缓存目录，先停止 watcher，并将项目索引偏好保存为关闭；构建或同步期间拒绝删除，避免 Windows 文件句柄冲突；
- 设置页新增稳定挂载的受控确认弹窗。确认后调用删除命令、刷新缓存统计并显示成功或失败反馈；
- 补充前端 API 调用测试。相关 Rust 测试 12/12、前端定向测试 11/11 与 TypeScript 检查通过。

**下一轮唯一优先任务**

- 实现暂停/恢复索引构建和更新，并明确 paused 状态下的查询降级行为。

### Iteration 0 — 2026-08-14 — 建立实施基线

**目标**

- 把搜索优化方案转化为可持续执行的工程文档；
- 明确阶段、边界、验收和降级原则。

**完成**

- 记录真实 22,317 文件、330.45 秒基线；
- 确立快速扫描 + FTS5 trigram 双引擎；
- 建立 M0～M5 里程碑和测试矩阵；
- 建立迭代协议和初始决策记录。

**遗留**

- 当前搜索核心缺少独立测试；
- ripgrep 相关 crates 尚未加入依赖；
- 尚未建立可重复性能基准。

**下一轮唯一优先任务**

- 完成 M0 的现有行为测试与固定测试仓库，为扫描器替换建立结果一致性护栏。

### Iteration 1 — 2026-08-14 — 一致性护栏与 ignore 遍历器

**目标**

- 为扫描器重构补齐现有搜索语义测试；
- 提供可重复生成固定规模测试仓库的工具；
- 移除目录遍历阶段逐路径调用 libgit2 ignore 的主要开销。

**完成**

- 新增普通字符串转义、大小写、全词、非法正则、include/exclude 优先级测试；
- 新增 Unicode UTF-16 列坐标和跨平台相对路径测试；
- 新增 Git ignore、内置目录排除的真实临时目录测试；
- 新增 `scripts/generate-search-fixture.mjs`，支持文件数、命中数和输出目录参数；
- 生成器只允许清理带专用 marker 的既有目录，并拒绝根目录、当前目录及其父目录；
- 引入 `ignore` 0.4.32，并确认许可证为 `Unlicense OR MIT`；
- 使用 `ignore::WalkBuilder` 处理 `.gitignore`、`.ignore`、`.rgignore` 和内置目录排除；
- 删除搜索遍历阶段的逐路径 `git2::Repository::status_should_ignore`；
- `cargo check --lib` 通过；
- 搜索定向测试 10/10 通过；
- 非沙箱环境 Rust 全量测试 245/245 通过；
- 固定仓库生成器完成 32 文件、7 命中烟测并安全清理。

**验证说明**

- 沙箱内全量测试有 19 个既有 Git 临时仓库测试因系统临时目录权限失败；相同测试在非沙箱环境全部通过；
- 全仓库 `cargo fmt --check` 会被既有 `mcp_servers.rs` 格式问题拦截，本轮修改的 `content_search.rs` 已单独通过 rustfmt。

**遗留**

- walker 当前仍为单线程；
- 文件内容仍使用 `fs::read` 整体载入；
- glob 仍保留旧的动态规划实现，尚未统一到 `globset`；
- 尚未记录固定 1k/20k/50k 仓库性能数据。

**下一轮唯一优先任务**

- 抽取纯文件匹配结果，使扫描可安全迁移到 `WalkParallel`，同时保持批次、上下文和取消语义。

### Iteration 2 — 2026-08-14 — 并行扫描与可重复性能基准

**目标**

- 将文件读取和内容匹配安全地并行化；
- 让生产搜索核心可脱离 Tauri UI 做集成测试和基准；
- 建立 1k、20k、50k 固定仓库的首轮性能数据。

**完成**

- 抽取 `FileSearchOutcome`，单文件扫描不再直接修改共享搜索上下文；
- 为上下文、UTF-16 坐标、二进制、非法 UTF-8、取消和截断补充测试；
- 使用 `WalkParallel` 并行处理文件，线程数为逻辑 CPU 的一半且限制在 1～6；
- 合并阶段强制执行全局 5,000 条上限，覆盖多文件并发竞态；
- 将 Tauri batch emitter 注入搜索核心，生产和测试使用同一条代码路径；
- 新增完整核心测试，验证批次、Git ignore、预取消和全局结果上限；
- 新增手动 ignored benchmark，可直接读取固定仓库生成器产物；
- 定向测试 16/16 通过，1 项手动性能测试默认忽略；
- `cargo check --lib` 通过。

**调试构建性能数据**

查询均为 `updateWrapper.`，100 个文件命中；数据来自同一机器、同一生成器和同一生产搜索核心：

| 生成文件 | 实际扫描文件 | 匹配 | 搜索核心耗时 |
| ---: | ---: | ---: | ---: |
| 1,000 | 1,002 | 100 | 71ms |
| 20,000 | 20,002 | 100 | 644ms |
| 50,000 | 50,002 | 100 | 1,454ms |

额外的 2 个文件是生成器 marker 和 `.gitignore`。这些数据来自小型 TypeScript 文本和热开发环境，只用于建立相对回归基线，不能替代真实大型仓库、冷缓存和大文件测试。

**遗留**

- 文件内容仍通过 `fs::read` 整体载入，尚未使用 `grep-searcher`；
- 结果合并使用短时互斥锁，尚未改为有界通道；
- 动态规划 glob 尚未替换为预编译 `globset`；
- 当前只支持完整 UTF-8，不支持 BOM 和 UTF-16；
- 尚未在截图中的真实 22,317 文件仓库复测。

**下一轮唯一优先任务**

- 引入 `grep-searcher`、`grep-regex` 和 `globset`，先统一并预编译过滤与匹配规则，再替换整文件读取。

### Iteration 3 — 2026-08-14 — 请求级预编译 glob

**目标**

- 消除旧 glob 实现对每个文件重复分配匹配状态的开销；
- 统一文件名模式、相对路径模式和大小写规则；
- 在读取项目文件前拒绝非法 glob。

**完成**

- 引入直接依赖 `globset` 0.4，实际锁定版本由 `ignore` 依赖图统一管理；
- 新增 `CompiledPatterns` 和 `SearchFilters`；
- include/exclude 每个请求只编译一次，并在线程间安全共享；
- 无 `/` 的模式匹配文件名，包含 `/` 的模式匹配规范化相对路径；
- exclude 保持高于 include 的优先级；
- 替换旧的逐字符动态规划 `wildcard_match`；
- 新增大小写、文件名、相对路径、include/exclude 和非法 glob 测试；
- 搜索定向测试 17/17 通过，1 项手动性能测试默认忽略；
- `cargo check --lib` 通过；
- 20k 固定仓库复测为 615ms，Iteration 2 同条件为 644ms。该差异较小，只作为未发生性能回退的证据。

**遗留**

- `SearchFilters` 尚未移动到独立模块；
- 用户 glob 与 Git ignore 的完整交互文档尚未编写；
- 文件读取仍为 `fs::read`，内容搜索器尚未切换到 ripgrep searcher；
- 尚未实现有界结果通道。

**下一轮唯一优先任务**

- 引入 `grep-searcher` 与 `grep-regex`，通过可回归的小步替换整文件读取，并验证上下文、二进制检测、UTF-16 列号和取消行为。

### Iteration 4 — 2026-08-14 — 流式内容扫描与编码兼容

**目标**

- 使用 ripgrep Rust 组件替换 `fs::read` + 全文件行向量；
- 保持现有 IPC、逐命中结果、上下文、取消和截断语义；
- 覆盖 UTF-8 BOM、UTF-16 BOM、CRLF anchor、二进制和非法 UTF-8 边界。

**完成**

- 引入 `grep-searcher` 0.1.17 与 `grep-regex` 0.1.14；
- 确认两个依赖均为 `Unlicense OR MIT`，来源为 ripgrep 官方仓库 crates；
- 每个并行 walker worker 创建并复用一个 `Searcher`，按文件流式读取固定缓冲；
- 自定义 `ContentSearchSink` 增量重建前后各 3 行上下文，不再保存完整文件和全部行切片；
- 候选器与精确匹配器共享同一请求 pattern，后者继续生成同一行内的多个结果与 UTF-16 列号；
- 候选器启用逐行 anchor 和 CRLF 模式，同时取消 matcher 专属行终止符，兼容 LF/CRLF 搜索器；
- NUL 二进制、命中行非法 UTF-8、I/O 失败按单文件跳过；
- 支持 UTF-8 BOM 和带 BOM 的 UTF-16 LE/BE 自动转码；
- 新增 UTF-8 BOM、UTF-16、重叠上下文组和 CRLF `^...$` 回归测试；
- 搜索定向测试 21/21 通过，1 项手动性能测试默认忽略；
- Rust 全量测试 256/256 通过，1 项手动性能测试默认忽略；
- `cargo check --lib` 通过。

**调试构建性能数据与取舍**

查询为 `updateWrapper.`，100 个文件命中；同一机器、同一生成器、热开发环境：

| 生成文件 | 实际扫描文件 | Iteration 3 | Iteration 4 |
| ---: | ---: | ---: | ---: |
| 20,000 | 20,002 | 615ms | 806ms |
| 50,000 | 50,002 | 1,454ms | 1,569ms |

极小文件夹具中，流式 searcher 增加了逐文件搜索状态和回调固定成本，因此 20k 微基准约回退 31%，50k 约回退 8%。该实现的收益是固定缓冲、编码支持和大文件内存上界，不应把这一轮描述为大量极小文件场景的纯速度提升。两组结果仍低于 M1 的 20k/3 秒目标；重复查询的主要加速仍依赖 M2 索引。

**遗留**

- 结果合并仍使用短时互斥锁，尚未改为有界结果通道；
- 快速扫描模块仍集中在单文件中，types、matcher、filters、scanner 尚未拆分；
- 尚未记录峰值内存和真实 22,317 文件项目复测数据；
- 索引数据库、查询路由和设置页状态均未实现。

**下一轮唯一优先任务**

- 完成 M1 的有界结果通道与取消传播，移除 worker 对全局搜索上下文互斥锁的高频合并依赖，然后进入 M2 索引 MVP。

### Iteration 5 — 2026-08-14 — 当前项目索引开关

**目标**

- 让“索引代码库以实现即时搜索”可以针对当前项目开启和关闭；
- 偏好跨应用重启持久化；
- 在索引引擎未完成时明确展示快速扫描降级，避免虚假 ready 状态。

**完成**

- 新增 `get_search_index_status` 与 `set_project_index_enabled` Tauri 命令；
- 项目偏好以规范化路径为键保存到主设置 SQLite 的 `searchIndex.projectPreferences`；
- Windows 项目键使用大小写不敏感规范化，关闭值也显式保存，为未来自动索引保留用户覆盖语义；
- 设置页根据当前项目异步加载状态，切换期间显示 loading 并阻止重复提交；
- 保存成功后显示消息，读取或保存失败时保留原状态并显示错误；
- 开启状态返回 `state = scan_only`、`backend = scan`，页面显示“快速扫描”；
- 无当前项目时禁用项目开关并提示先打开项目；
- “自动索引新文件夹”继续禁用，等待 preflight、磁盘预算和真实构建机制；
- 更新简体中文、繁体中文、英文和日文文案；
- 新增数据库偏好和前端 IPC 参数测试；
- 前端全量测试 328/328 通过，TypeScript `--noEmit` 检查通过，语言 JSON 校验通过；
- 前端生产构建通过；
- Rust 全量测试 258/258 通过，1 项手动性能测试默认忽略，`cargo check --lib` 通过。

**遗留**

- 开启偏好不会创建 FTS5 数据库，当前搜索行为仍是 Iteration 4 的快速扫描；
- 尚无 building、ready、failed、stale 等真实索引状态；
- 尚未实现重建、暂停和删除操作；
- 当前项目索引偏好暂存在主设置数据库，实际索引文件仍必须按设计存入独立缓存目录。

**下一轮唯一优先任务**

- 先由用户在 Tauri 应用中验证开关可切换、跨重启保留并显示“快速扫描”；确认交互无误后，再继续 M1 有界通道或 M2 FTS5 preflight。

### Iteration 6 — 2026-08-14 — FTS5 构建器与状态反馈

**目标**

- 开启项目索引后真正创建 FTS5 trigram 数据库；
- 后台构建期间提供可恢复、可轮询的真实状态；
- 任何失败、中断或半成品都不能影响快速扫描结果。

**完成**

- 按规范化项目路径的 SHA-256 建立 `<app_cache_dir>/search-index/v1/<project-key>/index.sqlite3` 独立缓存目录；
- 运行时创建 FTS5 contentless-delete + trigram 虚拟表，不支持时返回 `unsupported` 并保留扫描后端；
- preflight 复用 `.gitignore`、`.ignore`、`.rgignore`、Git global ignore 和内置目录排除规则；
- 默认单文件上限 5 MiB，不跟随符号链接，支持 UTF-8、UTF-8 BOM、UTF-16 LE/BE，跳过二进制、非法编码、权限错误和非 UTF-8 路径；
- 构建写入带 job id 的临时数据库，完成 FTS optimize、SQLite `integrity_check` 和 FTS 行数核对后才替换正式数据库；
- Windows 兼容的正式库→备份→新库替换流程会在激活失败时恢复旧库，启动读取状态时也会恢复遗留备份；
- 关闭开关可取消当前构建；重新构建会使旧 job 失效，过期 job 无法覆盖新状态；
- 状态包含 phase、处理/总文件数、已索引/跳过数、处理字节、索引大小、开始/更新时间和最近错误；
- 设置页每 700ms 轮询活动构建，展示预扫描、构建、最终化、就绪、失败和不支持状态，并提供重新构建操作；
- 新增 FTS5 真实子串查询、ignore、二进制和编码回归测试；
- 修复 preflight 后文件突然增长导致无界读取的风险，读取最多 5 MiB + 1 字节；
- Rust 定向索引测试 4/4 通过；Rust 全量测试 260/260 通过，1 项手动性能测试忽略；
- 前端全量测试 329/329 通过，语言 JSON、TypeScript `--noEmit` 和生产构建均通过。

**设计取舍与遗留**

- 当前 `ready` 表示索引文件完整可查询，不表示全文搜索已经切换到索引；页面明确说明查询仍使用快速扫描；
- 尚未实现 watcher、增量更新、stale/reconciliation，因此现在接入索引候选查询会有漏掉新文件的风险；
- 尚未实现删除索引、缓存预算、磁盘空间 preflight 和自动索引新项目；
- 构建器当前保存 preflight 候选路径向量，以换取构建期间一致的总数和进度；超大仓库的流式/分批候选队列留待性能迭代。

**下一轮唯一优先任务**

- 实现索引 watcher、构建期间变更重放和 stale/reconciliation；只有能够证明候选集完整后，再把普通长度至少 3 的文字查询路由到 FTS5，并继续用统一 matcher 精确验证。

### Iteration 7 — 2026-08-17 — 一致性 watcher 与 FTS5 查询路由

**目标**

- 让 ready 索引在项目变化后立即失效，禁止查询陈旧候选集；
- 合并文件事件并自动恢复到新的完整索引；
- 为安全查询启用 FTS5 候选路由，同时保留所有扫描降级路径。

**完成**

- 每个启用项目持有独立递归 watcher；原生 watcher 创建或监听失败时自动回退 2 秒 PollWatcher；
- 忽略 `.git`、`target`、`dist`、`build`、`node_modules` 等生成目录，`.git/info/exclude` 变化仍会触发范围重建；
- 代码变化会增加项目 revision，并在 ready 时立即切换为 `stale / scan`；500ms 静默窗口后自动启动原子全量重建；
- 构建开始前记录 revision；构建期间出现变化时，新数据库不会进入 ready 查询路径，而是标记 `replaying_changes` 并自动再构建；
- 应用重启后不直接信任磁盘上的旧 ready 元数据，而是先进入 reconciliation 后台重建；
- 普通非正则查询且 Unicode 字符数至少为 3 时，ready 索引使用 FTS5 trigram 生成最多 50,000 个候选文件；
- FTS5 查询按字面量转义，覆盖引号和 `OR` 等查询语法文本；候选路径拒绝根路径、绝对路径和 `..` 分量；
- 候选集生成前后核对 job id、状态和 revision；中途变化、候选过多、数据库异常、短查询、正则或非 ready 状态全部回退快速扫描；
- FTS5 只负责候选生成，原有 matcher 继续负责大小写、全词、逐行多命中、上下文和 UTF-16 列号；
- 搜索摘要新增 backend、fallback reason、候选文件数、索引 generation 和 index state，界面明确显示“FTS5 索引”或“快速扫描”；
- 设置页订阅 `search-index-status` 事件实时展示 stale/building/ready，构建轮询保留为恢复保障；事件订阅失败提供可见错误；
- 新增 watcher 过滤、Git exclude、FTS 查询转义和索引候选精确验证测试。
- Rust 全量测试 263/263 通过，1 项手动性能测试忽略；前端全量测试 329/329、语言 JSON、TypeScript `--noEmit` 和生产构建均通过。
- Windows NSIS 安装包已重新生成：`Termflow_1.8.19_x64-setup.exe`，SHA-256 `0BF9C55081E9303C128083AC37C4F8EB6500AABA68B0133D6D299C9322576DE1`。

**设计取舍与遗留**

- 当前变化恢复为防抖全量重建，不是最终的逐文件增量 SQL 更新；大型仓库频繁编辑时重建成本仍高；
- watcher 是最终一致性机制，文件写入与操作系统事件送达之间仍存在极短窗口；候选读取会在前后核对 revision，但无法锁住外部文件系统；
- PollWatcher 更兼容网络盘，但检测延迟最高约为轮询周期加防抖时间；
- 仍未实现 watcher 溢出的周期性低频 reconciliation、索引删除和缓存预算。

**下一轮唯一优先任务**

- 将普通文件 create/modify/remove/rename 从全量重建优化为单写 actor 的批量 FTS5 upsert/delete；ignore 文件变化、watcher rescan、事件溢出和大规模 Git 操作继续保留全量 reconciliation。

### Iteration 8 — 2026-08-17 — 底栏后台任务聚合与克隆并发

**目标**

- 将索引构建进度从设置卡片移动到始终可见的应用底栏；
- 保证索引与一个或多个 Git 克隆同时运行时互不覆盖；
- 在有限底栏宽度中保留完整详情和错误反馈。

**完成**

- `StatusBar` 全局订阅当前项目的 `search-index-status`，首次进入项目主动加载状态，并在 preflight/building/stale 期间每秒轮询兜底；
- 项目切换时按规范化项目路径隔离状态和错误，禁止上一项目的延迟事件闪现在新项目底栏；
- 设置页移除大进度条，保留开关、状态、文件统计、索引大小和手动重建；
- 单一索引任务或克隆任务直接在底栏显示；同时运行时显示总任务数、克隆与索引各自百分比和两条独立微型进度轨；
- 详情弹层同时列出当前索引和全部克隆任务，克隆取消按钮仍只作用于对应 task id；
- 索引监听失败、状态读取失败、FTS5 不支持和构建失败均保留可见的黄色错误入口；监听错误与轮询读取错误分开保存，成功轮询不会误清除监听故障；
- 四种语言文案和 JSON 校验通过；新增后台任务纯函数测试，覆盖单任务、1 克隆 + 1 索引、多个克隆 + 索引、ready 隐藏、错误可见和进度边界；
- 前端全量测试 333/333 通过，TypeScript 与生产构建通过（6533 modules）。
- Windows NSIS 安装包已重新生成：`Termflow_1.8.19_x64-setup.exe`，SHA-256 `51A026D003B18DE18AD3D8357172F379D836148236C85AE5111B967A5EC3DCCF`。

**两个任务同时运行时的风险结论**

- UI 层不再存在事件互相覆盖、错误取消任务或一项完成后隐藏另一项的问题；
- 克隆写目标仓库，索引写应用缓存中的临时 SQLite 后再原子替换，正常情况下没有文件写冲突；
- 同盘并发仍会竞争磁盘和 CPU，因此两边可能变慢；
- 如果用户把新仓库克隆到当前已索引项目的子目录中，watcher 会收到大量事件。现有 500ms 防抖和 revision 重放保证结果最终正确，但可能推迟 ready 或增加一次重建；后续单写增量 actor 应增加大批量变更识别和 I/O 调度。

**下一轮唯一优先任务**

- 将普通文件 create/modify/remove/rename 从全量重建优化为单写 actor 的批量 FTS5 upsert/delete，并为克隆落在当前项目子树的场景增加大批量事件阈值和延迟 reconciliation。

### Iteration 11 — 2026-08-17 — 移除分隔条键盘调整

**目标**

- 移除分隔条的 ↑/↓ 每次调整 5% 行为；
- 避免分隔条与搜索结果 ↑/↓ 导航形成两套作用不同的键盘操作；
- 保留鼠标拖动、安全边界和双击复位。

**完成**

- 删除分隔条的 ArrowUp、ArrowDown、Home 和 End 处理逻辑；
- 分隔条不再进入 Tab 焦点顺序，并移除对应 focus 样式；保留分隔语义和当前比例说明；
- 搜索输入框原有 ↑/↓ 结果导航和 Enter 打开行为保持不变；
- 鼠标拖动仍限制在 20/80～80/20，双击仍恢复默认 55/45；
- 四种语言 JSON 校验通过，前端全量测试 339/339 通过，TypeScript 与生产构建通过（6534 modules）；
- Windows NSIS 安装包已重新生成：`Termflow_1.8.19_x64-setup.exe`，SHA-256 `DC171734B934CF7622326FAAEFD1F64A349B3922F62CD5CCF973494F00FD0D4B`。

**下一轮唯一优先任务**

- 将普通文件 create/modify/remove/rename 从全量重建优化为单写 actor 的批量 FTS5 upsert/delete，并为克隆落在当前项目子树的场景增加大批量事件阈值和延迟 reconciliation。

### Iteration 10 — 2026-08-17 — 可拖动搜索分隔条

**目标**

- 允许用户按当前任务调整结果列表与代码预览高度；
- 保证拖动、键盘操作、边界和弹窗关闭场景稳定；
- 不改变搜索请求、结果选择和文件打开语义。

**完成**

- 中间横线升级为 8px 指针捕获分隔条，hover、focus 和拖动时显示主色手柄；
- 鼠标上下拖动实时更新比例，最小 20%、最大 80%，两侧始终保留可操作空间；
- 方向键上下每次调整 5%，Home/End 跳到边界，双击恢复默认 55/45；
- 分隔条提供 `role=separator`、方向、当前值和中英文辅助说明；
- 松开指针、pointer cancel、失去 pointer capture 或关闭弹窗都会清理拖动状态；
- 比例换算和边界逻辑抽为纯函数，覆盖正常位置、上下越界和无效容器尺寸测试；
- 四种语言 JSON 校验通过，前端全量测试 339/339 通过，TypeScript 与生产构建通过（6534 modules）。
- Windows NSIS 安装包已重新生成：`Termflow_1.8.19_x64-setup.exe`，SHA-256 `D4BC1C8BC8ED67C773A0938FAE7DA6A07368CAEA13EC1EBADECB969BEF3B2380`。

**下一轮唯一优先任务**

- 将普通文件 create/modify/remove/rename 从全量重建优化为单写 actor 的批量 FTS5 upsert/delete，并为克隆落在当前项目子树的场景增加大批量事件阈值和延迟 reconciliation。

### Iteration 9 — 2026-08-17 — IDEA 风格上下搜索布局

**目标**

- 将全文搜索从左右分栏调整为上方结果、下方预览；
- 为结果文本和代码预览提供完整横向空间；
- 保持键盘导航、打开行为、流式结果和搜索状态不变。

**完成**

- 搜索内容区改为单列两行网格，上方结果占 55%，下方预览占 45%；
- 原来的竖向分隔线改为横向分隔线，结果列表和代码预览继续独立滚动；
- 移除窄窗口隐藏预览的降级逻辑，小窗口仍保持上下结构，仅收紧结果区内边距；
- 选中项、双击打开、方向键移动、Enter 打开和 Ctrl+Enter 保留搜索窗口的状态逻辑未改动。
- 前端全量测试 333/333 通过，TypeScript 与生产构建通过（6533 modules）。
- Windows NSIS 安装包已重新生成：`Termflow_1.8.19_x64-setup.exe`，SHA-256 `BDD35929DDCEE2ADC18DB180D1CADC470002C5FA0D32E9135CC2DF3FEC5712CD`。

**下一轮唯一优先任务**

- 将普通文件 create/modify/remove/rename 从全量重建优化为单写 actor 的批量 FTS5 upsert/delete，并为克隆落在当前项目子树的场景增加大批量事件阈值和延迟 reconciliation。
