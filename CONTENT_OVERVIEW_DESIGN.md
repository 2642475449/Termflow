# Termflow「内容概览」初步设计文档

> 状态：Draft 0.1  
> 目标版本：MVP  
> 设计对象：AI Agent 终端中的长回答  
> 暂定名称：内容概览（Content Overview）

## 1. 背景

Termflow 当前通过 xterm 展示 Claude Code、Codex、OpenCode 等 Agent CLI 的原始终端输出。当回答较长时，用户容易遇到三个问题：

1. 阅读前无法快速判断回答是否覆盖了自己的目标；
2. 阅读中容易迷失位置，难以定位某个章节；
3. 回看历史回答时，需要重新滚动并通读大量内容。dd

因此，希望在不改变终端原生体验的前提下，为当前长回答提供一个按需打开的小窗口，用于显示摘要、要点和内容结构，并支持快速跳转。

## 2. 产品目标

### 2.1 核心目标

- 让用户在 5 秒内理解当前长回答的主旨；
- 让用户从概览直接跳转到感兴趣的内容位置；
- 保持轻量：默认不占用终端阅读空间，不打断 Agent 输出；
- 兼容多种 Agent CLI，而不是只服务 Claude Code。

### 2.2 非目标

- 不替代原文，也不把摘要作为事实依据；
- MVP 不提供全文改写、知识库沉淀或跨会话总结；
- MVP 不追求对所有终端程序进行内容理解；
- 不在回答生成过程中频繁调用模型实时重写摘要。

## 3. 目标用户与典型场景

### 场景 A：阅读前预判

Agent 输出了一份数千字的设计、评审或排查报告。用户打开“内容概览”，先看一句话总结和关键结论，再决定是否完整阅读。

### 场景 B：长文导航

用户只想查看“技术架构”或“实施计划”，点击概览中的章节后，终端滚动到对应位置。

### 场景 C：历史回看

用户重新打开旧会话，通过已有的概览快速恢复上下文，无需从头通读。

## 4. 交互方案

### 4.1 入口

- 入口放在当前终端面板 TabBar 的右侧操作区，使用“文档 + 闪光/列表”图标；
- 文案与 Tooltip 统一为“内容概览”；
- 仅当当前面板是支持的 AI Agent 会话时显示；普通 PowerShell/CMD 默认不显示；
- 当可概览文本低于阈值时，按钮保持可见但弱化，Tooltip 显示“当前内容较短”。

选择面板级入口而不是全局标题栏入口，是因为 Termflow 支持分屏；概览必须明确属于当前 pane 中的当前 session。

### 4.2 打开方式

- 点击入口，在按钮下方打开 Popover；
- 建议宽度 `360–400px`，最大高度为当前窗口高度的 `60%`；
- Popover 固定在所属终端面板右上角，不随终端内容滚动；
- 点击外部、再次点击入口或按 `Esc` 关闭；
- 第一版不使用常驻侧栏，避免压缩终端宽度并触发大量换行重排。

### 4.3 信息结构

Popover 自上而下包含：

1. **标题栏**：内容概览、生成时间、关闭按钮；
2. **一句话总结**：1–2 句，优先表达结论而不是复述背景；
3. **关键要点**：最多 4 条，每条尽量不超过两行；
4. **内容结构**：3–8 个可点击章节，展示当前阅读位置；
5. **底部操作**：重新生成、复制摘要；必要时展示“摘要可能不完整”。

示例：

> 建议以现有科研管理系统为基础，增加统一的统计分析与可视化报表能力，并按科学性、合理性、针对性组织方案。

- 方案围绕三个评审维度展开；
- 推荐采用渐进式建设，而不是替换现有系统；
- 内容覆盖多维统计、图表、导出和安全实施；
- 提供三种方案对比并给出推荐路径。

章节：需求理解 → 建设定位 → 方案对比 → 技术架构 → 报表设计 → 实施计划。

### 4.4 状态设计

| 状态 | 表现 |
| --- | --- |
| 尚无长内容 | 空状态：“当前还没有需要概览的长回答” |
| Agent 正在输出 | 展示上一版摘要，并提示“回答仍在生成”；首次打开时可先展示本地提取的结构 |
| 正在生成摘要 | Skeleton；已提取的章节可以先显示并跳转 |
| 生成成功 | 展示总结、要点和章节 |
| 原文继续变化 | 标记“内容已更新”，用户可手动刷新；回答完成后自动刷新一次 |
| 生成失败 | 保留本地章节导航，并提供“重试” |
| 内容可能被截断 | 明确提示“仅基于当前可读取内容生成” |

## 5. 内容识别与生成策略

### 5.1 当前技术约束

Termflow 接收的是 `pty-output` 原始字符流，并写入 xterm，而不是结构化的 user/assistant message。因此不能简单地读取“最后一条 assistant message”。输出还可能包含：

- ANSI 颜色与光标控制序列；
- Spinner、状态行和被覆盖的文本；
- Tool 调用日志、命令回显和终端提示符；
- 表格、代码块和软换行；
- xterm scrollback 上限导致的历史截断。

### 5.2 MVP 推荐链路

1. **按 session 缓存原始 PTY 输出片段**：在现有 `pty-output` 监听链路旁增加有界环形缓冲区；
2. **确定本轮边界**：以 `submitAgentTurnInput` 作为起点，以 Agent hook 的 `assistant_complete` / session 状态变为 completed 作为优先终点；
3. **降级边界**：缺少 hook 时，使用“用户提交后至下一次可识别提示符/稳定空闲”的区间；
4. **终端文本归一化**：解析 ANSI 和光标操作得到接近 xterm 最终呈现的纯文本，合并软换行；
5. **噪声过滤**：过滤启动 Banner、状态行、明显的 Spinner、重复工具进度和 shell prompt；
6. **本地结构提取**：识别 Markdown 标题、编号标题、短行标题和列表层级，立即生成目录；
7. **AI 摘要**：只发送清洗后的本轮回答，返回严格结构化 JSON；
8. **位置映射**：保留章节到 xterm buffer 行号/文本锚点的映射，点击章节时滚动定位；
9. **缓存**：使用 `sessionId + turnId + contentHash + summaryVersion` 作为缓存键。

### 5.3 为什么不直接读取屏幕可见区域

可见区域只包含当前 viewport，无法覆盖完整长回答；而直接拼接原始 PTY chunk 又会保留覆盖字符和控制序列。MVP 应以“本轮有界输出缓存 + 终端语义归一化”为主，xterm buffer snapshot 作为校验或降级来源。

### 5.4 摘要输出结构

```ts
interface ContentOverview {
  sessionId: string;
  turnId: string;
  contentHash: string;
  summary: string;
  keyPoints: Array<{
    id: string;
    text: string;
    anchorId?: string;
  }>;
  sections: Array<{
    id: string;
    title: string;
    level: 1 | 2 | 3;
    anchorText: string;
    bufferLine?: number;
  }>;
  coverage: "complete" | "partial";
  generatedAt: number;
}
```

### 5.5 AI 提示词原则

- 仅根据输入原文总结，不补充外部事实；
- 一句话总结优先写结论、建议和限制；
- 关键要点最多 4 条，去重，不使用空泛描述；
- 章节标题尽量复用原文标题，避免导航与原文不一致；
- 返回 JSON，字段长度设硬限制；
- 原文不足或截断时返回 `coverage: partial`。

## 6. 视觉规范（初稿）

- 复用 Termflow 的浅色玻璃拟态卡片、圆角与阴影变量；
- 面板宽 `380px`，圆角 `12px`，内边距 `14–16px`；
- 标题 13px/600，正文 12–13px，辅助信息 11px；
- 一句话总结使用轻微品牌色背景块，但不使用大面积高饱和颜色；
- 关键要点采用中性圆点，不使用过多彩色图标；
- 当前章节使用左侧 `2px` 品牌色标记；
- 章节 hover 时出现浅色底，明确可点击；
- 深色主题沿用现有 token，不单独写死颜色。

## 7. 性能、隐私与成本

- 默认仅在用户首次打开概览或回答完成时生成，不对每个 PTY chunk 调用模型；
- 内容低于建议阈值（初始建议：`800` 个中文字符或 `1,500` 个英文字符）时，不自动生成 AI 摘要；
- 环形缓冲区设置字符/字节上限，避免长会话持续占用内存；
- 发送摘要前展示与现有 Agent/模型一致的隐私策略；如果摘要使用不同服务，必须单独告知；
- 对疑似密钥、Token、环境变量值做基础脱敏；
- 摘要结果按会话存储，可在设置中关闭持久化或完全关闭该功能；
- 同一 `contentHash` 不重复生成，控制成本。

## 8. MVP 范围

### 必须具备

- 面板级“内容概览”入口与 Popover；
- 捕获并清洗当前一轮 Agent 回答；
- 一句话总结、最多 4 个关键要点；
- 本地章节提取和点击跳转；
- loading、失败、partial、内容更新状态；
- session/turn 级缓存；
- Claude Code 作为首个完整支持对象。

### 可延后

- Codex、OpenCode 等不同 CLI 的精细噪声过滤适配；
- 自动跟随当前阅读章节；
- 用户调整摘要长度；
- 摘要搜索、导出和跨会话聚合；
- 可拖拽、可钉住的常驻面板；
- 对代码块、表格和引用的专门概览。

## 9. 验收标准

1. 对超过 3 屏的 Claude Code 中文回答，用户可在两次点击内看到概览；
2. 摘要面板打开不改变终端尺寸，不导致终端重新换行；
3. 回答完成后首次摘要在目标机器上可接受时间内出现，期间目录先行可用；
4. 点击章节后能定位到正确内容附近，允许误差不超过约 3 个终端行；
5. 摘要不包含明显的 ANSI、Spinner、shell prompt 或启动 Banner；
6. 回答未完成或内容被截断时，界面明确提示，不把摘要伪装成完整结果；
7. 分屏时，每个 Popover 只作用于所属 pane 的当前 session；
8. 功能关闭后不发起摘要请求，也不额外持久化回答正文。

## 10. 风险与待验证问题

### 高风险

- **回答边界不稳定**：不同 Agent hook 能力不同，需要 provider adapter 和可靠降级策略；
- **终端归一化复杂**：直接正则删除 ANSI 不能正确处理覆盖输出，宜复用 xterm parser/buffer 语义；
- **跳转锚点漂移**：窗口 resize 会改变换行与 buffer 行号，需要文本锚点二次校验；
- **隐私边界**：终端输出可能包含密钥、日志或客户数据，摘要服务来源必须透明。

### 待产品确认

- 摘要默认使用当前 Agent，还是 Termflow 内置的统一模型；
- 是否只在用户主动点击后生成，还是长回答完成后后台预生成；
- 历史会话中保存摘要但不保存额外正文是否满足预期；
- 普通终端中用户选中文本后，是否也允许“概览所选内容”。

## 11. 建议迭代路径

### Phase 0：可用性验证

用固定长回答数据完成 Popover、目录提取、点击定位和各种状态，不接真实摘要服务。

### Phase 1：Claude Code MVP

接入 turn 边界、文本清洗、按需 AI 摘要与缓存，重点验证准确性、隐私提示和性能。

### Phase 2：多 Agent 适配

为 Codex、OpenCode 等建立 provider adapter，并基于真实样本完善噪声过滤与完成事件。

### Phase 3：增强体验

加入当前章节跟随、固定侧栏、选区概览、导出和历史概览等增强能力。

## 12. 初步埋点

- `content_overview_opened`
- `content_overview_generated`
- `content_overview_generation_failed`
- `content_overview_section_clicked`
- `content_overview_copied`
- `content_overview_regenerated`

重点观察：长回答中的入口点击率、章节跳转率、生成失败率、重复生成率，以及打开后快速关闭的比例。

## 13. 初步效果图

下图用于验证信息层级、浮窗尺寸和入口位置，不作为最终视觉稿。图中终端原文由生图模型重绘，实际实现应完全保留 xterm 渲染结果。

![Termflow 内容概览初步效果图](./public/images/content-overview-mockup.png)
